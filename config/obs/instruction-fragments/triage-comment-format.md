**Issue comment format (mandatory for triage):**
- Start every `add_comment` body with the heading `## Issue Triage` (exactly that title; nothing else on that line).
- Place the heading before the `tl;dr` / recommendation sections.
