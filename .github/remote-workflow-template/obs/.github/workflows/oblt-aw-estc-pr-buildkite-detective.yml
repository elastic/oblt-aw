name: Observability Agentic Workflow — PR Buildkite Detective

on:
  status:

permissions:
  contents: read

jobs:
  run-aw:
    if: >-
      github.event.state == 'failure' &&
      contains(github.event.context, 'buildkite')
    permissions:
      actions: write
      checks: read
      contents: write
      discussions: write
      id-token: write
      issues: write
      pull-requests: write
    uses: elastic/oblt-aw/.github/workflows/oblt-aw-estc-pr-buildkite-detective.yml@main # ratchet:exclude
    secrets:
      COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}
      BUILDKITE_API_TOKEN: ${{ secrets.BUILDKITE_LOGS_API_TOKEN }}
