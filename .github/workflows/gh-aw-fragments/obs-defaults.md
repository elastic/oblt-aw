---
model: gpt-5.3-codex
---

## Observability defaults

Shared opinionated defaults for Observability-owned agentic workflows. Import this fragment from each in-repo `gh-aw-*.md` so the model (and future shared defaults) stay in one place.
